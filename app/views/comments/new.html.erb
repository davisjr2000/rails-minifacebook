<h1>Add Comment</h1>
  <%= simple_form_for [@post, @comment] do |f| %>
    <%= f.input :content %>
    <%= f.association :user %>
  <%= f.submit "Post comment", class: "btn btn-primary" %>
  <% end %>
